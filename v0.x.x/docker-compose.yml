version: "2.4"

services:
  app:
    image: swingcarspointca:0.5.6
    build: 
      context: .
    ports:
      - "80:80"
    command: node app.js
    volumes:
      - .:/app
    environment:
      - NODE_ENV=development