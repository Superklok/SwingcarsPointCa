version: "3.8"

services:
  app:
    image: superklok/swingcarspointca:0.5.12
    build: 
      context: .
    ports:
      - "80:80"
    command: node app.js
    volumes:
      - .:/app
    environment:
      - NODE_ENV=production