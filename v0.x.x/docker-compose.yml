version: "2.4"

services:
  app:
    image: swingcarspointca:0.4.5
    build: 
      context: .
    ports:
      - "80:80"
    command: node app.js
    volumes:
      - .:/app
    environment:
      - NODE_ENV=development